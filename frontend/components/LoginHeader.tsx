const LoginHeader = () => {
  return (
    <div
      className={`w-screen flex justify-center 
          p-3 font-semibold
          `}
      style={{ backgroundColor: "#00B2FF", color: "#fff" }}
    >
      <h1 className={`font-anek font-semibold`}>Bouticão PetShop</h1>
    </div>
  );
};

export default LoginHeader;
